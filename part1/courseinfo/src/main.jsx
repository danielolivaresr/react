import ReactDOM from 'react-dom/client'

import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(<App />) // Define un componente de React con el nombre App.
                                                                     // .render(<App />) renderiza su contenido dentro del elemento div, definido en HTML, que tiene como id 'root'.
                                                                     // Para acceder al ID ROOT del HTML, hacemos uso de getElementById, utilizando el DOM.
                                                                    